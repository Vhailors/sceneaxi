# Compatibility declaration for pnpm below 10.6; newer pnpm reads `nodeLinker` from
# pnpm-workspace.yaml instead. Both declarations must stay hoisted; the version split
# and package-shape contract are owned by ../../docs/websites-deploy.md.
node-linker=hoisted
