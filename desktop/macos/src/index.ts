/** macOS packaging seam for the existing Engine Desktop application. */
import type { PackageSeam } from "@sceneaxi/schemas";

export const seam: PackageSeam = Object.freeze({
  name: "@sceneaxi/desktop-macos",
  releaseGroup: "desktop",
});
